.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
# Local data artifacts (keep large data off git and off /home; see CF_DATA_ROOT).
# Anchored to repo root so it does NOT match the src/conceptformer/data/ package.
/data/
*.sqlite
*.jsonl
*.parquet
wandb/
